# Core runtime deps (MVP). Pin loosely; lock for production as needed.
# Install: pip install -r requirements.txt
pydantic>=2.6
pyyaml>=6.0
typer>=0.12
rich>=13.0
jinja2>=3.1
requests>=2.31
anthropic>=0.40
yt-dlp>=2024.0
pillow>=10.0

# --- Optional heavy extras (install only for the full pipeline) ---
# Transcription (Stage 2):
#   pip install whisperx faster-whisper
# Active-speaker reframing (Stage 5):
#   pip install mediapipe opencv-python numpy
#
# System binaries (NOT pip): ffmpeg + ffprobe must be on PATH.
#   macOS:  brew install ffmpeg
#   Ubuntu: sudo apt-get install ffmpeg
