# Local-only files. Real student data must never be committed.
config.json
config.json.bak
config/privacy-terms.txt
logs/
recon/
node_modules/

# Per-district persistent browser profiles (cookies/sessions)
.profiles/

# In case profileDir is ever pointed inside the project folder
profile/
.teachtown-runner/
