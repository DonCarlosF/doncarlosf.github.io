import { NotFoundBody } from "@/components/blocks/NotFoundBody";

// notFound() thrown from a slug page renders here, already inside the site layout.
export default function SiteNotFound() {
  return <NotFoundBody />;
}
